export default {
  // maxAgeAccessTokenInCookie: 1000 * 60 * 15,
  maxAgeAccessTokenInCookie: 1000 * 15,
  // maxAgeRefreshTokenInCookie: 1000 * 60 * 60 * 24 * 7,
  maxAgeRefreshTokenInCookie: 1000 * 30,
};
